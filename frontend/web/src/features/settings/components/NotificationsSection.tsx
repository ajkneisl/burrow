import { Card } from "burrow-core"


/**
 * Settings involving notifications.
 */
export default function NotificationSettings() {
    return (
        <Card className="flex flex-col gap-4">
            <p>this will be here eventually...</p> s
        </Card>
    )
}
